/**
 * Shared types barrel.
 * - common: app-wide common utility/UI-agnostic types
 * - entities: DB schema-derived entity types
 * - api: transport contracts between API routes and UI
 */
export type * from './api';
export type * from './common';
export type * from './entities';
